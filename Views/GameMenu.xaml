<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="GameMenuButton" BasedOn="{StaticResource {x:Type ButtonEx}}" TargetType="ButtonEx">
        <Setter Property="Margin" Value="16" />
        <Setter Property="MinWidth" Value="400" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Background="{TemplateBinding Background}" Margin="-10">
                        <Border x:Name="MainBackground"
                            CornerRadius="6"
                            Opacity="0" Background="{DynamicResource ControlBackgroundLightBrush}" 
                        />
                        <Border x:Name="MainBorder" Margin="-6"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                            Style="{DynamicResource SolidBorderLightStyle}" Tag="False"
                            BorderThickness="3" Visibility="Collapsed" />
                        <TextBlock x:Name="Text" Style="{DynamicResource ButtonTextBlockBaseStyle}"
                            Foreground="{TemplateBinding Foreground}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Margin="10,0" Padding="0,0,0,8">
                            <ContentPresenter />
                        </TextBlock>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>

                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                            <Setter Property="Opacity" Value="0.5" TargetName="MainBackground" />
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.5" TargetName="MainBackground" />
                            <Setter Property="Tag" Value="True" TargetName="MainBorder" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <!-- <Setter Property="Foreground" Value="Transparent" /> -->
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GameMenuBorder" TargetType="Border">
        <Setter Property="CornerRadius" Value="16" />
        <Setter Property="MinWidth" Value="400" />
        <Setter Property="Padding" Value="10,10,8,10" />
        <Setter Property="Background" Value="{DynamicResource OverlayMenu}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
        <Style.Triggers>
            <EventTrigger RoutedEvent="Window.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" Duration="0:0:0.3" From="0" To="1" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <DataTemplate x:Key="GameMenuHeaderTemplate">
        <Border BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource ControlBorderBrush}">
            <TextBlock Text="{Binding Name}"
                 Style="{DynamicResource GameTitleHeaderText}"
                 VerticalAlignment="Bottom"
                 HorizontalAlignment="Center"
                 TextWrapping="Wrap"
                 Padding="15,15,15,10"
                 Margin="0,0,0,10"
                 Background="Transparent"
            />
        </Border>
    </DataTemplate>

    <Style x:Key="GameMenuIconStyle" TargetType="ContentControl" >
        <Setter Property="Focusable" Value="False" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="10,0,20,-8" />
        <Setter Property="Width" Value="{DynamicResource FontSizeLarge}" />
    </Style>

    <DataTemplate x:Key="GameMenuPlayButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <ContentControl 
                x:Name="PlayItemIcon"
                Style="{DynamicResource GameMenuIconStyle}"
                Content="{DynamicResource GameMenuIconPlay}" />
            <TextBlock x:Name="PlayItemText" Text="{Binding}" VerticalAlignment="Center" />
        </StackPanel>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{PluginSettings Plugin=BackToGame, Path=IsRunning}" Value="True">
                <Setter Property="Content" Value="{DynamicResource GameMenuIconBackToGame}" TargetName="PlayItemIcon"/>
                <Setter Property="Text" Value="{DynamicResource LOCBackToGame}" TargetName="PlayItemText"/>
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <DataTemplate x:Key="GameMenuInstallButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <ContentControl 
                Style="{DynamicResource GameMenuIconStyle}"
                Content="{DynamicResource GameMenuIconInstall}" />
            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="GameMenuFavoriesButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <ContentControl 
                Style="{DynamicResource GameMenuIconStyle}"
                Content="{DynamicResource GameMenuIconFavories}" />
            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="GameMenuHdrButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <ContentControl 
                Style="{DynamicResource GameMenuIconStyle}"
                Content="{DynamicResource GameMenuIconHdr}" />
            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="GameMenuVisibilityButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <ContentControl 
                Style="{DynamicResource GameMenuIconStyle}"
                Content="{DynamicResource GameMenuIconVisibility}" />
            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="GameMenuSetFieldsTemplate">
        <StackPanel Orientation="Horizontal">
            <ContentControl 
                Style="{DynamicResource GameMenuIconStyle}"
                Content="{DynamicResource GameMenuIconSetFields}" />
            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="GameMenuRemoveButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <ContentControl 
                Style="{DynamicResource GameMenuIconStyle}"
                Content="{DynamicResource GameMenuIconRemove}" />
            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="GameMenuUninstallButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <ContentControl 
                Style="{DynamicResource GameMenuIconStyle}"
                Content="{DynamicResource GameMenuIconUninstall}" />
            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="GameMenuCustomActionButtonTemplate">
        <StackPanel Orientation="Horizontal">
            <ContentControl 
                Style="{DynamicResource GameMenuIconStyle}"
                Content="{DynamicResource GameMenuIconCustomAction}" />
            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="GameMenuExtensionsTemplate">
        <StackPanel Orientation="Horizontal">
            <ContentControl 
                Style="{DynamicResource GameMenuIconStyle}"
                Content="{DynamicResource GameMenuIconExtensions}" />
            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
        </StackPanel>
    </DataTemplate>

    <DataTemplate x:Key="GameMenuCompletionStatusTemplate">
        <StackPanel Orientation="Horizontal">
            <ContentControl 
                Style="{DynamicResource GameMenuIconStyle}"
                Content="{DynamicResource GameMenuIconCompletionStatus}" />
            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>